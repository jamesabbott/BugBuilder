1) origin rejoining with SIS joins contigs too far apart since no consideration paid to distances...
2) No scaffolds.fasta generated if assembly merging used and no scaffolder selected, causing annotation_merge to fail
